# Raspberry Pi 5 - Edge Tracker Requirements

# Core ML/CV libraries
opencv-python==4.10.0.84
numpy==1.26.4
onnxruntime==1.19.2

# Camera interface (installed via apt, but including for reference)
# picamera2  # Install via: sudo apt install python3-picamera2

# HTTP requests
requests==2.31.0

# Model conversion (for download_yolo.py script)
ultralytics>=8.0.0

# Optional: For better progress bars during downloads
# tqdm==4.66.5
